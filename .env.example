# =============================================================================
# Cortex Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# The setup script (setup.ps1) does this automatically.
# =============================================================================

# Database
# If using Docker Compose (recommended), this default works out of the box.
# If using your own PostgreSQL, update host/port/credentials accordingly.
DATABASE_URL=postgresql://postgres:cortex-dev@localhost:5432/cortex

# AI API Keys (required for research, planning, and scorecard features)
OPENAI_KEY=sk-your-openai-key-here
TAVILY_API_KEY=tvly-your-tavily-key-here

# Authentication
# IMPORTANT: Change JWT_SECRET to a random string in production.
# Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-me-to-a-random-64-char-string
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Server
PORT=3000
HOST=0.0.0.0
LOG_LEVEL=info

# Environment
NODE_ENV=development
